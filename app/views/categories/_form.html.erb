<div class="container"><hr>

  <a class="btn btn-primary" href="#" role="button"><%= link_to 'Markers', markers_path %> </a>
  <a class="btn btn-primary" href="#" role="button"><%= link_to 'Types', types_path %> </a>
  <a class="btn btn-primary" href="#" role="button"><%= link_to 'Api Json', api_json_path %> </a>
  <a class="btn btn-primary" href="#" role="button"> <%= link_to 'Dashboard', root_path %> </a>

  <hr>


  <%= form_with(model: category, remote: true, data: { confirm: '¿Desea confirmar?'}) do |form| %>
    <% if category.errors.any? %>
      <div id="error_explanation">
        <h2><%= pluralize(category.errors.count, "error") %> prohibited this category from being saved:</h2>

        <ul>
        <% category.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
        </ul>
      </div>
    <% end %>
    
    <div class="field">
      <%= form.label :name %>
      <%= form.text_field :name %>
    </div>

    <div class="field">
      <%= form.label :type_id %>
      <%= form.collection_select :type_id , Type.all, :id, :name, prompt: true  %>
    </div>

    <div class="field">
      <%= form.label :public %>
      <%= form.select :public,[false,true] %>
    </div>

    <div class="actions">
      <%= form.submit data: {disable_with: "Creando"} %>
    </div><hr>
  <% end %>
</div>